Pull the latest .ipynb from GitHub and convert it back to the local .py file.

Steps to follow exactly:

1. Run `git pull` to fetch the latest changes from GitHub.

2. Run `python nb_to_py.py bhuvanfitterpierce.ipynb bhuvanfitterpierce.py` to overwrite the local .py with the notebook's content.

3. Report how many cells were converted and confirm the local .py is now up to date.
